it( "has a route handler listening to /api/admin/posts/create for post request", async () => {

} );

it( "can only accessed if the user is signed in", async () => {

} );

it( "returns a status other than 401 if the user is signed in", async () => {

} );

it( "returns a status other than 403 if the user is authorized", async () => {

} );

it( "returns an error if an invalid title is provided", async () => {

} );

it( "creates a post with valid inputs", async () => {

} );

it( "publishes an event", async () => {

} );
